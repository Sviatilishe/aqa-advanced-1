// Given values
const height = 15;
const radius = 7;

// Calculation of the cylinder volume
const cylinderVolume = Math.PI * Math.pow(radius, 2) * height;
console.log("Volume Cylinder:", cylinderVolume.toFixed(2));