const firstArray = [1, 2, 3, 4, 5];
const secondArray = [' Once I Caught A Fish Alive'];

const concatenatedArray = firstArray.concat(secondArray);

console.log(`Concatenated array: ${concatenatedArray}`);
